name: Update posts

on:
  push:
    branches: [ master ]
    paths-ignore:
      - 'projects'
      - 'assets'
#  pull_request:
#    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
    - name: Updating content
      run: |
        python __scripts/update_posts.py
    #- name: Commit files
    #  run: |
    #    echo ${{ github.ref }}
    #    git add .
    #    git config --local user.email "action@github.com"
    #    git config --local user.name "GitHub Action"
    #    git commit -m "Automated content creation push" -a | exit 0
    #- name: Push changes
    #  #if: github.ref == 'refs/heads/master'
    #  uses: ad-m/github-push-action@master
    #  with:
    #    branch: master
    #    github_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Commit to repository
      uses: oleksiyrudenko/gha-git-credentials@v1
      with:
        name: 'philclausen'
        email: 'phil.clausen@gmail.com'
        actor: 'philclausen'
        token: '${{ secrets.TOKEN }}'
    - run: |
        git config user.email 'phil.clausen@gmail.com'
        git config user.name 'philclausen'
        git config user.password ${{ secrets.TOKEN }}
        # Checkout the branch so we can push back to it
        git checkout master
        git add .
        # Commit and push
        git commit -m "Automated content creation push"; git push origin master
